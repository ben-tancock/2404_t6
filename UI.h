#ifndef UI_H
#define UI_H

#include "defs.h"
#include "Animal.h"
#include <string>


class UI
{
  public:
    UI();
    void addBird(int);
    void addChicken(int);
    void addPig(int);
    void addCat(int);
    void dance();
    //int numAnimals;
    //void initData();
    //Animal* barn[MAX_ANIMALS];

  protected:
    
  private:
   
};

#endif
